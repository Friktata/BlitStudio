///////////////////////////////////////////////////////////////////////////////
//  BlitStudio/src/core/configTemplate.config.js
//
//

    export const ConfigTemplate = (() =>
    {

        const   _config = {
            
            // Config options go here.

        };

        return structuredClone(_config);

    })();
